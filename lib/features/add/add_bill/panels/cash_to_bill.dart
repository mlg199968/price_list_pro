import 'package:flutter/material.dart';

class CashToBill extends StatelessWidget {
  const CashToBill({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.lightBlueAccent,
      child:Center(
        child: Text("Cheque panel"),
      ),
    );
  }
}